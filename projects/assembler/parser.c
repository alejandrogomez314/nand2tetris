#include <stdbool.h>
#include <stdlib.h>
#include <stdio.h>
#include "parser.h"
#include "symbolTable.h"

bool parser(char *processed_file, struct keytab_list *variables) {
  int i = 0;
  while (processed_file[i] != EOF) {
    // delimit by new line
    
  }
  return true;
}
